function setup() {
  
//create a surface to draw on
 createCanvas(600, 600);
  
  background(29, 110, 161);
  strokeWeight(10);
  stroke('white');
  //point(300,300);
  frameRate(1000)
  
}

function draw() {
  //DECLARE VARIABLES
  point(mouseX,mouseY)
  point(300,300)

  

}
